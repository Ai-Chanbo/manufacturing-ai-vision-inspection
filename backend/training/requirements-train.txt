# EfficientAD 学習・ONNXエクスポート専用の依存（推論サーバー backend/requirements.txt とは分離）
#
# 方針:
#   - 推論サーバー(backend/app)に学習用の重い依存を持ち込まないため、別 venv で使用する。
#   - venv は --system-site-packages で作成し、ベース環境の torch / torchvision (CPU wheel) を再利用する。
#   - 動作確認環境: Python 3.14 / torch 2.12.0+cpu / torchvision 0.27.0+cpu / CPU only
#
# セットアップ例（backend/training で実行）:
#   python -m venv --system-site-packages .venv-train
#   .venv-train/Scripts/python -m pip install -r requirements-train.txt
#
# anomalib は EfficientAD ONNX エクスポートの安定性を考慮し 2.4.2 にピン留め。
anomalib==2.4.2

# torch / torchvision はベース環境の CPU wheel を再利用する（--system-site-packages）。
# 単独環境で導入する場合のみ以下を有効化:
# torch==2.12.0
# torchvision==0.27.0
